/**
 * OutputBar module barrel export
 *
 * Exports the main OutputBar component and related types.
 * Internal sub-components and hooks are not exported to maintain encapsulation.
 */

export { OutputBar } from './OutputBar';
export type { OutputBarProps, OutputBarViewState } from './types';
